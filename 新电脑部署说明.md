# TrafficDetect 新电脑部署说明

## 1. 文档用途
本文档用于指导在一台新的 Windows 电脑上部署并启动 TrafficDetect 项目。

适用场景：

- 在另一台电脑上本地运行项目
- 用于答辩演示或项目展示
- 不重新训练模型，只需要把项目跑起来

如果你的目标只是启动网页端进行识别演示，按照本文档操作即可。

## 2. 部署前需要准备的内容
请先确保你已经拿到完整项目目录，建议至少包含以下内容：

- `app/`
- `configs/`
- `models/`
- `static/`
- `weights/`
- `requirements.txt`
- `start_web.py`

其中最重要的是 `weights/` 目录，至少需要包含：

- `weights/best.pt`
- `weights/animal_best.pt`

如果缺少权重文件，项目可以启动，但对应模块无法进行识别。

## 3. 新电脑的软件环境要求

### 3.1 操作系统
推荐：

- Windows 10
- Windows 11

### 3.2 Python 版本
推荐：

- Python 3.10

不建议一开始使用过新的 Python 版本，以免部分依赖兼容性不稳定。

### 3.3 环境管理工具
推荐使用：

- Anaconda
或
- Miniconda

## 4. 安装 Conda 环境

### 4.1 打开终端
可以使用以下任一种：

- Anaconda Prompt
- PowerShell
- Windows Terminal

### 4.2 创建项目环境
进入任意目录后执行：

```powershell
conda create -n yolov8 python=3.10 -y
```

### 4.3 激活环境

```powershell
conda activate yolov8
```

如果命令执行成功，终端前面通常会出现 `(yolov8)`。

## 5. 进入项目目录
假设项目被放在桌面，例如：

```powershell
cd C:\Users\你的用户名\Desktop\yolov8
```

你也可以把项目放在其他位置，但后续所有命令都需要在项目根目录执行。

## 6. 安装项目依赖
确认已经进入项目根目录并激活 `yolov8` 环境后，执行：

```powershell
pip install -r requirements.txt
```

安装完成后，建议简单检查几个关键库是否可用：

```powershell
python -c "import torch, ultralytics, fastapi, uvicorn; print('环境正常')"
```

如果终端输出“环境正常”，说明核心依赖已经安装成功。

## 7. 启动前检查
在正式启动前，请检查以下内容。

### 7.1 检查配置文件
确认以下文件存在：

- `configs/default.yaml`

这是项目默认读取的运行配置文件。

### 7.2 检查权重文件
确认以下文件存在：

- `weights/best.pt`
- `weights/animal_best.pt`

### 7.3 检查前端资源
确认以下目录存在：

- `static/`

### 7.4 检查后端代码
确认以下目录存在：

- `app/`
- `models/`

## 8. 启动项目

### 8.1 推荐启动命令
在项目根目录下执行：

```powershell
conda activate yolov8
python start_web.py --host 0.0.0.0 --port 8010
```

### 8.2 启动成功的标志
如果启动成功，终端一般会出现类似信息：

```text
INFO:     Open in browser: http://127.0.0.1:8010
INFO:     Alternate local URL: http://localhost:8010
```

或者出现 Uvicorn 正常运行的信息。

## 9. 浏览器访问地址
启动后，在浏览器打开以下任一地址：

- `http://127.0.0.1:8010`
- `http://localhost:8010`

如果是局域网内其他设备访问，还可以使用当前电脑的本机 IP 地址加端口访问。

## 10. 如果要换端口
如果 `8010` 端口被占用，可以换成其他端口，例如：

```powershell
python start_web.py --host 0.0.0.0 --port 8020
```

那么浏览器访问地址就改为：

- `http://127.0.0.1:8020`
- `http://localhost:8020`

## 11. 常见问题排查

### 11.1 提示找不到 `uvicorn`
原因：

- 没有激活正确的 Conda 环境
或
- 依赖没有安装完整

解决方法：

```powershell
conda activate yolov8
pip install -r requirements.txt
```

### 11.2 提示找不到模型权重
原因：

- `weights/` 目录下缺少 `best.pt` 或 `animal_best.pt`

解决方法：

- 检查项目是否完整拷贝
- 确认权重文件名称没有被改动

### 11.3 网页能打开但识别失败
原因可能包括：

- 权重文件缺失
- 配置文件被误改
- 上传文件格式不支持

建议检查：

- `configs/default.yaml`
- `weights/best.pt`
- `weights/animal_best.pt`

### 11.4 端口被占用
如果提示端口冲突，可以换一个端口重新启动，例如：

```powershell
python start_web.py --host 0.0.0.0 --port 8020
```

### 11.5 依赖安装很慢
可以尝试使用国内镜像，例如：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 12. 建议的标准部署流程
在新电脑上，建议严格按下面顺序操作：

1. 安装 Anaconda 或 Miniconda
2. 打开终端
3. 创建环境：`conda create -n yolov8 python=3.10 -y`
4. 激活环境：`conda activate yolov8`
5. 进入项目目录
6. 安装依赖：`pip install -r requirements.txt`
7. 检查 `weights/` 和 `configs/default.yaml`
8. 启动项目：`python start_web.py --host 0.0.0.0 --port 8010`
9. 浏览器访问 `http://localhost:8010`

## 13. 部署完成后的建议
部署成功后，建议你额外做两件事：

### 13.1 先测试一张图片
先上传一张本地图片，确认模型可以正常识别。

### 13.2 准备答辩演示图片
如果你要做答辩演示，优先使用已经筛选好的演示集：

- `datasets/bdd100k_demo/images`

这样效果会更稳定。

## 14. 一句话总结
在新电脑部署这个项目，核心就是四步：

1. 创建并激活 `yolov8` 环境
2. 执行 `pip install -r requirements.txt`
3. 确认 `weights/` 和 `configs/default.yaml` 完整
4. 执行 `python start_web.py --host 0.0.0.0 --port 8010`
