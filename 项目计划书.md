# TrafficDetect 项目计划书

## 1. 项目名称与目标

### 1.1 项目名称
TrafficDetect

### 1.2 总体目标
本项目旨在构建一个面向道路交通场景的双模块智能识别系统，完成从数据集处理、模型训练、推理部署到网页端可视化展示的完整工程闭环。项目不以单一算法验证为目标，而强调“算法设计 + 系统实现 + 本地部署 + 服务器训练 + 展示交付”的一体化落地能力。

### 1.3 模块目标
- 模块一：BDD100K 交通目标检测
- 模块二：道路动物识别

其中：
- 模块一承担项目主成果任务
- 模块二承担非常规道路风险目标识别扩展任务

## 2. 项目背景与研究意义
随着自动驾驶、智能交通和道路场景理解研究的发展，交通视觉感知已经成为计算机视觉的重要应用方向。传统交通目标如车辆、行人、交通灯和交通标志，是道路感知系统中的基础识别对象；而道路中的非常规风险目标，如动物、临时障碍物和突发异物，虽然出现频率较低，但对交通安全同样具有现实意义。

本项目采用“双模块”设计，研究意义主要体现在以下三点：

1. 兼顾常规交通目标与特殊风险目标  
模块一负责标准交通场景检测，模块二面向非常规道路风险识别，能够形成更完整的道路感知体系。

2. 兼顾算法优化与工程落地  
项目不仅完成模型训练，还实现了后端接口、网页可视化、批处理和本地部署方案，提升了工程完整性。

3. 体现方案演进与问题驱动优化能力  
模块二在项目推进过程中经历了多轮技术路线调整，最终从 SOD 异物分割转向 IDD animal 单类检测，体现了面向真实效果做决策的工程思路。

## 3. 总体技术路线
TrafficDetect 的总体技术路线由数据层、模型层、服务层和表现层组成。

### 3.1 数据层
- BDD100K 原始标注转换为 YOLO 检测格式，用于模块一训练
- IDD Detection 中筛选 `animal` 类，并清洗极小目标，生成模块二训练数据

### 3.2 模型层
- 模块一基于 YOLOv8s 进行结构级改进，形成 baseline、P2、P2+CBAM、full 四条实验路线
- 模块二采用 YOLO11n 轻量检测模型，实现单类动物检测

### 3.3 服务层
- 后端基于 FastAPI 搭建统一服务接口
- 提供单图检测、实时视频流、序列批处理和模块配置查询能力

### 3.4 表现层
- 前端采用 HTML + JavaScript 静态页面
- 支持两个模块切换
- 支持检测结果可视化、置信度展示、序列预览与结果下载

## 4. 模块一设计与实现

### 4.1 数据集
模块一使用 BDD100K 检测数据，并通过数据转换脚本重建为标准 YOLO 检测格式。最终目录结构为：

- `datasets/bdd100k/images/train`
- `datasets/bdd100k/images/val`
- `datasets/bdd100k/labels/train`
- `datasets/bdd100k/labels/val`

### 4.2 类别设置
模块一识别 10 类交通目标：

- pedestrian
- rider
- car
- truck
- bus
- train
- motorcycle
- bicycle
- traffic light
- traffic sign

### 4.3 算法优化设计
模块一是本项目算法优化的重点。为了避免仅使用标准 YOLOv8s 带来的小目标检测不足问题，项目采用了逐步增强的结构优化路线。

#### 4.3.1 baseline：标准 YOLOv8s
对应变体：

- `baseline`

作用：

- 作为项目的基础对照组
- 提供后续结构优化的参考基线

#### 4.3.2 P2 小目标检测头优化
对应模型文件：

- `configs/model_yolov8s_p2.yaml`

设计目的：

- 针对远距离行人、交通灯、交通标志等小目标的识别难点
- 在原始 YOLOv8s 的基础上增加更高分辨率的 P2 检测分支

技术价值：

- 提高浅层细粒度特征参与检测的能力
- 强化小目标的空间细节表达
- 降低原始模型对小目标的漏检风险

#### 4.3.3 CBAM 注意力机制优化
对应模型文件：

- `configs/model_yolov8s_custom.yaml`

设计目的：

- 在复杂交通背景中，普通卷积特征容易受到无关背景干扰
- 引入 CBAM 后，可从通道和空间两个维度强化关键特征响应

技术价值：

- 提升模型对关键目标区域的关注能力
- 抑制复杂背景、遮挡和干扰区域带来的误判
- 改善特征融合阶段的信息利用效率

#### 4.3.4 WIoU 边界框损失优化
对应注册逻辑：

- `models/register.py`

设计目的：

- 标准检测损失在复杂目标定位时存在一定局限
- 在 `full` 变体中引入 WIoU 作为边界框回归损失

技术价值：

- 优化边界框回归质量
- 提高定位精度
- 使最终模型不仅能检测到目标，还能给出更稳定的定位框

### 4.4 模型演进路径
模块一支持如下四条实验路线：

- `baseline`
- `p2`
- `p2_cbam`
- `full`

演进关系为：

1. `baseline`：标准 YOLOv8s  
2. `p2`：在 baseline 上增加 P2 小目标检测头  
3. `p2_cbam`：在 P2 结构基础上加入 CBAM 注意力机制  
4. `full`：在 `p2_cbam` 基础上进一步启用 WIoU

这种设计方式有两个优点：

- 能在论文和答辩中清楚展示每一步改进的依据
- 能在工程实现中保持结构可解释、实验可复现

### 4.5 训练与推理实现
核心脚本包括：

- `training/train.py`
- `training/val.py`
- `inference/predict.py`

模块一支持：

- 单图检测
- 实时视频流检测
- 网页端可视化展示

### 4.6 模块一定位
模块一是本项目的主要成果模块。其任务定义清晰、数据规模充分、训练链路成熟，能够稳定支撑毕业设计中的主要算法、实验和展示内容。

## 5. 模块二设计与实现

### 5.1 初版思路：SOD 道路异物方向
模块二最初的目标是实现道路异物识别。项目在早期选择了 SOD 数据集，并首先尝试将其转换为 YOLO 检测格式，用于训练单类 `obstacle` 检测模型。

### 5.2 中期重写：SOD 分割路线
由于 SOD 检测路线效果不理想，项目曾进一步将模块二重写为道路异物分割模块，具体包括：

- 掩膜转 YOLO segmentation 标签
- 基于 `YOLOv8s-seg` 的分割训练脚本
- 掩膜叠加显示、面积统计和序列处理页面

### 5.3 路线调整的实验结论
从实验结果看，SOD 路线存在以下问题：

- 异物目标过小，检测与分割都较难稳定收敛
- 分割边界碎、噪声多，导致训练前期指标起量慢
- 即使训练轮数增加，实际展示效果提升仍然有限
- 作为答辩演示模块，稳定性不足

因此，继续死磕 SOD 路线的性价比较低。

### 5.4 最终决策：转向 IDD animal 单类轻量检测
结合老师提出的“小动物识别”建议，以及项目整体答辩需求，模块二最终转向：

- 数据集：IDD Detection 的 `animal` 子集
- 模型：YOLO11n
- 任务：单类 `animal` 检测
- 展示主题：道路动物识别

### 5.5 当前最终版本
模块二当前最终形态为：

- 轻量检测模型 `YOLO11n`
- 单类道路动物识别
- 单图检测
- 序列图片批处理
- 网页端结果展示

该方案相较于 SOD 分割更轻量、更稳定，也更适合作为扩展模块进行展示。

## 6. 模块二方案演进记录

### 6.1 SOD 检测尝试
优点：

- 能快速搭建单类道路异物检测原型
- 与模块一检测范式一致，工程接入相对方便

问题：

- 泛化效果不理想
- 测试图像中漏检明显

### 6.2 SOD 分割重写
优点：

- 保留了原始掩膜信息
- 技术路线更贴近原始数据形式

问题：

- 前期训练指标提升缓慢
- 分割结果不稳定
- 演示效果不够理想

### 6.3 放弃继续死磕分割的原因
- 小目标异物任务本身难度高
- 工程时间有限，继续深挖未必能转化为稳定展示效果
- 模块二更需要“可演示、可部署、可解释”的结果

### 6.4 转向 IDD animal 的原因
- 更贴近老师提出的“小动物识别”方向
- 相比极小异物，动物目标更容易形成稳定视觉特征
- 更适合轻量模型训练与网页演示
- 能与模块一区分开，形成“常规交通目标 + 非常规风险目标”的双模块结构

### 6.5 动物子集筛选与清洗规则
从 IDD Detection 中提取 `animal` 子集时，采用了以下原则：

- 仅保留包含 `animal` 标注的图像
- 删除图像中所有非 `animal` 类目标
- 对极小、难以观察的动物目标进行过滤

### 6.6 极小目标过滤标准
为确保训练样本更适合实际演示和检测，采用以下阈值：

- 最小框宽度：`20 px`
- 最小框高度：`20 px`
- 最小框面积：`900 px`

这样做的目的是：

- 清除“肉眼几乎不可见”的动物目标
- 提升训练样本质量
- 让模型学习的目标更加明确

## 7. 数据集处理方案

### 7.1 BDD100K 转 YOLO
模块一的数据处理流程为：

1. 读取原始图像与标注
2. 过滤无关目标
3. 转换为 YOLO 检测标签
4. 建立标准 `images/` 与 `labels/` 结构

相关脚本：

- `data/bdd100k_to_yolo.py`
- `data/verify_dataset.py`
- `data/dataset_stats.py`

### 7.2 IDD animal 筛选与 YOLO 转换
模块二的数据处理流程为：

1. 从 IDD Detection 中筛出 `animal` 图像
2. 删除非 `animal` 标注
3. 过滤极小目标
4. 重新划分 `train/val/test`
5. 转换为 YOLO 检测格式

相关脚本：

- `data/idd_animal_to_yolo.py`

### 7.3 数据集目录组织
模块一：

- `datasets/bdd100k`

模块二：

- `datasets/idd_animal_yolo`

### 7.4 划分原则
数据集划分遵循以下原则：

- 训练集用于模型学习
- 验证集用于模型选择与训练过程监控
- 测试集用于最终独立评估

模块二的数据重新划分，解决了原始筛选后 `test` 样本不足的问题。

## 8. 系统架构与功能设计

### 8.1 后端接口
后端基于 FastAPI，实现统一接口服务。核心接口包括：

- `/api/config`
- `/api/obstacle/config`
- `/api/detect/image`
- `/api/obstacle/detect/image`
- `/api/obstacle/detect/batch`
- `/api/ws/video`

### 8.2 网页功能设计
网页前端支持：

- 模块切换
- 单图输入
- 结果可视化
- 指标面板展示
- 模块一实时流
- 模块二序列批处理

### 8.3 两个模块的功能差异
模块一：

- 侧重常规交通目标检测
- 支持单图和实时视频流

模块二：

- 侧重道路动物识别
- 支持单图和批量序列处理

### 8.4 权重与结果目录规则
训练结果通常保存在：

- `results/<run_name>/weights/best.pt`

同时复制到固定位置：

- 模块一：`weights/best.pt` 或对应变体权重
- 模块二：`weights/animal_best.pt`

## 9. 环境与部署方案

### 9.1 本地环境
本地环境建议使用：

- Windows 10/11
- Conda 环境 `yolov8`
- Python 3.10 或 3.12

### 9.2 本地部署流程
本地部署是本项目展示环节中的重点内容，应作为答辩时的重要工程成果进行说明。

#### 9.2.1 创建环境

```powershell
conda create -n yolov8 python=3.10 -y
conda activate yolov8
```

#### 9.2.2 安装依赖

```powershell
pip install -r requirements.txt
```

#### 9.2.3 准备权重
需要在 `weights/` 下准备：

- 模块一主权重：`best.pt`
- 模块二主权重：`animal_best.pt`

#### 9.2.4 启动网页
推荐直接使用批处理脚本：

```powershell
start_web.bat
```

或命令行：

```powershell
python start_web.py
```

默认访问地址：

- `http://127.0.0.1:8010`
- `http://localhost:8010`

#### 9.2.5 本地部署检查重点
答辩前应重点确认：

1. 环境已激活
2. 权重文件存在
3. 端口可用
4. 页面能正常加载
5. 模块一与模块二都能返回检测结果

### 9.3 5090 服务器环境
服务器训练建议采用：

- Python 3.12
- NVIDIA RTX 5090
- 镜像自带 torch

### 9.4 上传与训练
推荐将整个项目目录压缩上传。

本地压缩命令：

```powershell
cd C:\Users\89657\Desktop
Compress-Archive -Path .\yolov8 -DestinationPath .\yolov8.zip -Force
```

服务器解压命令：

```bash
cd /root/autodl-tmp
apt-get update
apt-get install -y unzip tmux
unzip yolov8.zip
cd /root/autodl-tmp/yolov8
```

### 9.5 模块二服务器训练命令

```bash
cd /root/autodl-tmp/yolov8
source /root/miniconda3/etc/profile.d/conda.sh
conda create -n yolov8 python=3.12 -y
conda activate yolov8
pip install ultralytics==8.4.36 opencv-python==4.13.0.92 pillow==12.1.1 numpy==2.2.6 matplotlib==3.10.8 pandas==2.3.3 PyYAML==6.0.3 onnx==1.18.0 fastapi uvicorn python-multipart
unset OMP_NUM_THREADS
python -u training/train_obstacle.py --epochs 100 --batch 32 --workers 8 --imgsz 960 --device 0 --name train_animal_5090
```

## 10. 实验过程与结果分析

### 10.1 模块一训练效果与答辩价值
模块一的数据与任务高度匹配，经过多轮结构优化后，能够作为本项目最成熟、最稳定的成果模块。其答辩价值主要体现在：

- 有明确的基线模型
- 有清晰的结构优化路线
- 有可展示的检测效果
- 有完整的训练、推理和部署链路

### 10.2 模块二不同路线的实验结果
模块二共经历三条路线：

1. SOD 检测
2. SOD 分割
3. IDD animal 单类检测

前两条路线虽然实现了训练与网页接入，但实验结果显示其综合效果不理想。最终的 IDD animal 路线更轻量、更稳定，更适合作为扩展成果。

### 10.3 为什么最终选择当前模块二方案
最终方案的选择依据主要包括：

- 更贴合老师提出的小动物识别方向
- 检测比分割更轻量
- 更适合网页部署和本地演示
- 更容易形成稳定、直观的展示效果

### 10.4 项目整体完成度评价
当前项目已完成：

- 数据集处理
- 模型训练脚本
- 验证与推理脚本
- FastAPI 后端
- 前端网页展示
- 本地部署方案
- 服务器训练方案

因此，项目已经具备完整交付与答辩展示能力。

## 11. 问题与后续优化方向

### 11.1 模块二继续扩展的方向
- 引入更多道路场景动物类别
- 扩展到多类道路风险目标识别
- 尝试中等规模模型提升精度

### 11.2 模块一进一步优化空间
- 深化小目标检测策略
- 在更多训练设置上做系统对照实验
- 补充更细致的类别级分析

### 11.3 系统级扩展方向
- 增加日志管理和结果回溯机制
- 完善批处理结果统计与导出格式
- 扩展前端交互与演示方式

## 12. 结论
TrafficDetect 最终完成了一个双模块道路交通场景智能识别系统：

- 模块一面向常规交通目标检测，是本项目的主成果
- 模块二面向道路动物识别，是本项目的扩展成果

项目在推进过程中，模块二经历了从 SOD 异物检测到 SOD 分割，再到 IDD animal 单类检测的路线调整。这一演进过程体现了项目在真实实验结果驱动下不断优化方案的能力。最终系统不仅完成了模型训练，也完成了本地部署、网页展示和服务器训练方案设计，能够较好支撑毕业设计的保存、复现与答辩展示。
